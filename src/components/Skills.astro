---
import skills from "../data/skills.json";
---

<div>
  <h2 class="text-xl font-semibold mb-4">Stack tecnológico</h2>
  <ul class="grid grid-cols-1 sm:grid-cols-2 gap-4">
    {
      skills.map((skill) => (
        <li class="bg-slate-800/60 rounded-lg p-4 shadow border border-slate-700 hover:bg-slate-800 transition">
          <div class="text-lg font-medium text-white">{skill.nombre}</div>
          <div class="text-sm text-gray-400">{skill.nivel}</div>
        </li>
      ))
    }
  </ul>
</div>
