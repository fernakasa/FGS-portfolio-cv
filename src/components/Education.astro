---
import education from '../data/education.json';
---

<div>
  <h2 class="text-3xl font-semibold mb-8 text-left pl-16">
    🎓Educación
  </h2>

  <div class="flex flex-wrap justify-center items-center gap-8">

  <div class="relative border-l border-gray-700 ml-4 pl-6 space-y-12">
    {education.map((item, index) => (
      <div class="relative">
        <!-- Punto en la línea -->
        <div class="absolute -left-[31px] top-1.5 w-3 h-3 bg-yellow-400 rounded-full border-2 border-slate-950"></div>

        <!-- Contenido -->
        <div>
          <h3 class="text-yellow-400 font-semibold">{item.degree}</h3>
          <p class="text-white font-bold">{item.institution}</p>
          <p class="text-sm text-gray-400 mb-2">{item.period} · {item.location}</p>
          {/* <p class="text-gray-300 leading-relaxed">{item.description}</p> */}
        </div>
      </div>
    ))}
  </div>
  </div>
</div>
