---
import experience from '../data/experience.json';
---

<div>
  <h2 class="text-3xl font-semibold mb-8 text-left pl-16">
    💼Experiencia
  </h2>

  <!-- Wrapper para línea continua -->
  <div class="relative grid grid-cols-9 gap-4">

    <!-- Línea vertical única en el centro -->
    <div class="absolute left-1/2 transform -translate-x-1/2 w-[1px] h-full bg-gray-700 z-0" />

    <!-- Contenido -->
    <div class="col-span-9 space-y-12 z-10">
      {experience.map((item, index) => (
        <div class="grid grid-cols-9 gap-4 items-start">

          <!-- Col 1: Datos alineados a la derecha -->
          <div class="col-span-4 text-right">
            <h3 class="text-yellow-400 font-semibold text-xl">{item.title}</h3>
            <p class="text-white font-bold text-pretty">{item.company}</p>
            <p class="text-sm text-gray-400">{item.date}</p>
            <p class="text-xs italic text-gray-400">{item.type}</p>
          </div>

          <!-- Col 2: Punto en la línea -->
          <div class="col-span-1 flex justify-center relative">
            <div class="w-3 h-3 bg-yellow-400 rounded-full z-10 mt-1" />
          </div>

          <!-- Col 3: Descripción alineada a la izquierda -->
          <div class="col-span-4 text-left">
            <p class="text-gray-300 leading-relaxed">{item.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>
